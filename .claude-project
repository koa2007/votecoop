# .claude-project — marker для discovery + дашборда koa2007.top/dashboard
name: Spilka
display_name: Spilka (VoteCoop) — голосування в ОСББ / житлових кооперативах
aliases: [spilka, votecoop, votingapp]
status: active

# Короткі поля для картки дашборда
stage: prod · код стабільний (phase16 + пост-рев'ю фікси)
blocker: немає (по коду); бізнес — поки немає платящих клієнтів
next: brain докотить SQL #3/#6/#7 (admin-less group, group_stats leak, schema.sql sync) → далі маркетинг

code_path: D:\claudeprojects\votingapp
per_project_memory: D:\claudeprojects\votingapp\CLAUDE.md
zep_group_id: votecoop

# Де живе прод
prod:
  domain: https://spilka.top
  hosting: VPS 173.242.50.141 — nginx віддає статику з /opt/spilka-web, HTTPS Let's Encrypt
  repo: github.com/koa2007/votecoop (public, branch main, push → авто-деплой GitHub Actions)
  backend: PocketBase 0.39.4 (Go + SQLite) на тому ж VPS, служба pocketbase.service
  db_access: SQLite /opt/pocketbase/pb_data/data.db (тільки через SSH, вхід за ключем ~/.ssh/spilka_vps)
  email: Resend mail.spilka.top (DNS pending до AWS SES, SMTP ще не підключено)
  dns: adm.tools, домен 2013594
  admin_account: koa2007@gmail.com (єдиний адмін, хардкод у is_admin() SQL + app.isAdmin() JS)

stack:
  language: javascript (vanilla, без фреймворку, без bundler)
  frontend: single app-object у js/app.js (~5500 рядків) + PWA + i18n (uk/en/ru)
  backend: PocketBase (SQLite) + серверні хуки на JS у pb_hooks/
  hosting: власний VPS (nginx)
  payments: немає

domain: https://spilka.top

# Де що лежить + нюанси (читати щоб знати, де розкиданий проєкт)
notes: |
  ДЕ ЩО ЛЕЖИТЬ:
  • Код (локально): D:\claudeprojects\votingapp — index.html, js/app.js (вся логіка),
    js/supabase.js (обгортка БД), js/config.js (URL + публічний anon-ключ, безпечно),
    css/style.css, service-worker.js (зараз spilka-v18), supabase/*.sql (міграції phase2..phase16).
  • Репозиторій: github.com/koa2007/votecoop — гілка main, push авто-деплоїть на spilka.top.
  • База даних: SQLite /opt/pocketbase/pb_data/data.db на VPS. Схема — у tests/fixtures/collections.json.
    Правила доступу живуть у самих колекціях PocketBase; серверна логіка — у pb_hooks/.
  • Доступи (поза репо, локально): D:\claudeprojects\superbase-db.txt (пароль Postgres),
    D:\claudeprojects\superbase.txt (логін у Dashboard), .claude/CREDENTIALS.md (карта доступів).
  • Спільна память двох агентів: D:\claudeprojects\Obsidian\autonomus-memory\projects\votecoop\
    (profile.md — стабільні факти; log.md — журнал координації laptop ↔ brain).

  НЮАНСИ:
  • Два агенти працюють паралельно: я (Claude на ноуті) + autonomus-brain (VPS, через Telegram).
    Brain має прямий psql-доступ ($VOTECOOP_DB) і сам накатує SQL-міграції (dry-run BEGIN/ROLLBACK → COMMIT).
  • Анон-ключ у js/config.js публічний і це нормально — дані захищає RLS, не секретність ключа.
  • Адмінка PocketBase /_/ закрита ззовні — тільки через SSH-тунель на 127.0.0.1:8090.
  • Деплой: просто git push у main, GitHub Actions сам публікує. PWA-кеш бампиться версією SW.
  • status НЕ frozen — проєкт активний, код стабільний; "frozen" на старій картці було помилкою.
