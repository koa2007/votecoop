# Spilka — Design Principles (as-built)

> Цей документ описує **реально втілений** дизайн Spilka (не початковий замисел).
> Він має пріоритет над `design-system.md` (v1.0, березень 2026) там, де вони
> розходяться. `design-system.md` лишається як історичний замисел.
>
> Розбіжності замислу і реальності (замисел → факт):
> - Кнопки: чорний `#1A1A1A` → **синій iOS `#007aff`**
> - Іконки: Lucide → **Phosphor Icons** (CDN)
> - Чекбокси/радіо: кастомні 22px → **нативні** контроли
> - Модалки: центровані → **bottom-sheet** (висуваються знизу)
>
> Створено: 2026-06-23 на основі аудиту коду.

---

## 1. Філософія (лишається з v1.0)

- **Mobile-first** — телефон головний пристрій; контейнер ≤ 480px, центрований на desktop.
- **Мінімалізм і чистота** — нічого зайвого, довіра до системи (це інструмент голосувань, не іграшка).
- **Зрозумілість кожному** — нетехнічний користувач (мешканець ОСББ) має розібратись без інструкції.
- **Цілісність** — усі екрани з одного набору токенів і компонентів; нічого «зібраного з різних місць».

---

## 2. Колір — ТІЛЬКИ через токени (iOS-палітра)

**Правило #1: жодних hex/rgb прямо в правилах компонентів.** Лише `var(--color-*)`.
Виняток — бренд-кольори третіх сторін (напр. Google `#4285f4`) і визначення самих токенів у `:root`.

Джерело істини — блок `:root` у `css/style.css`. Ключові токени:

| Роль | Токен | Light |
|---|---|---|
| Фон екрана | `--color-bg` | `#f5f5f7` |
| Поверхня (картки) | `--color-surface` | `#ffffff` |
| Вторинна поверхня | `--color-surface-secondary` | `#f0f0f2` |
| Текст | `--color-text` | `#1c1c1e` |
| Вторинний текст | `--color-text-secondary` | `#6c6c70` |
| Межі | `--color-border` | `#e5e5ea` |
| **Акцент (primary)** | `--color-primary` | **`#007aff`** (iOS blue) |
| Успіх / Прийнято | `--color-success` | `#34c759` |
| Помилка / Відхилено | `--color-danger` | `#ff3b30` |
| Увага | `--color-warning` | `#ff9500` |
| В процесі | `--color-pending` | `#ffcc00` |

- **Акцент — синій `#007aff`.** Жодного індиго/фіолетового (`rgba(99,102,241,…)` — заборонено, це залишок старої палітри).
- **Темна тема — обовʼязкова парність.** Кожен колір компонента береться з токена, що має `[data-theme="dark"]` override. Новий компонент без темного варіанту = баг.

---

## 3. Типографіка

- **Шрифт:** системний стек `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`. Один на весь застосунок. `monospace` — лише для кодів груп.
- **Шкала розмірів** (впорядкувати ad-hoc значення до неї):

| Роль | Розмір | Вага |
|---|---|---|
| Заголовок екрана / великі цифри | 28px / 22px | 700 / 600 |
| Заголовок секції, модалки | 18px | 600 |
| Назва картки / поля вводу | 16px | 600 / 400 |
| Основний текст | 15–16px | 400 |
| Вторинний / підписи полів | 13px | 600 (label) |
| Мета, дати, підказки | 12px | 400 |
| Найдрібніше (nav-label) | 11px | 500 |

- **Правило: один розмір на одну роль.** Не змішувати 13px і 14px для підписів, 11px і 12px для дрібного, 15px і 16px для тіла. (Зараз є дрейф — див. аудит.)
- **Поля вводу — мінімум 16px** (інакше iOS зумить на фокусі).

---

## 4. Простір, радіуси, тіні

- **Простір — сітка 4/8:** 4, 8, 12, 16, 20, 24, 32. Уникати позасіткових (7, 10, 18px) без причини.
- **Радіуси — лише токени** `--radius-sm(8)/md(12)/lg(16)/xl(20)`. Круги — `50%`. Пігулки-бейджі — уніфікувати (зараз 20px/999px/4px вроздріб).
- **Тіні — лише токени** `--shadow-sm/md/lg`. Картки — `--shadow-sm`.

---

## 5. Компоненти (як побудовано)

- **Кнопки.** `--color-primary` фон для головної дії, повна ширина у формах. Мінімальна висота **44px** (tap-target). Варіанти: `.btn-primary`, `.btn-secondary`, `.btn-danger`. Іконкові — `.btn-icon` (44×44). *Не плодити* нові майже-однакові класи (зараз 5 схожих «прозорих іконкових кнопок» — консолідувати).
- **Картки.** База: `background var(--color-surface)` + `var(--shadow-sm)` + `:active{transform:scale(0.98)}`. **Радіус — `--radius-lg` (16px) для всіх карток** (зараз картка учасника випадає на 12px — звести до 16).
- **Форми.** Підпис над полем (`.field-label`, 13px/600) + поле на повну ширину + за потреби `.field-hint` (12px) під полем. Цей патерн (з join-форми) — **канон для всіх форм**; старий `.form-group label` (14px) поступово привести до нього. Фокус — рамка `--color-primary` + focus-ring.
- **Чекбокси/радіо — нативні** (рішення: не кастомізувати, системний вигляд ОК). Підсвітка обраного — рамка + фон від `--color-primary`.
- **Модалки — bottom-sheet:** оверлей `--color-overlay`, висування знизу (`slideUp`), заголовок + `.btn-icon` закриття, футер з кнопками `flex:1`. Закриття по кліку на оверлей.
- **Нижня навігація** — 4 вкладки, активна = `--color-primary`, іконки Phosphor.

---

## 6. Іконки — Phosphor, один стиль

- **Єдина система — Phosphor Icons.** `<i class="ph ph-*">` (outline) і `ph-fill` для заповнених станів.
- **Не змішувати з гліфами-символами.** Закриття «✕» → `ph-x`; назад «←» → `ph-arrow-left` (узгодити всі екрани).
- **Емодзі як UI-іконки — свідомий виняток лише для ролей:** 👍 голосуючий / 👁️ спостерігач. Це навмисна семантика (читається миттєво). Решта емодзі (🔔 ✏️ 🗑️ ⚙️ 📖) у кнопках/списках — привести до Phosphor. Зокрема 🔔 у списку сповіщень має збігатися з архівом (там уже `ph-bell`).

---

## 7. Доступність (hard rules)

- **Tap-target ≥ 44px** для будь-якого інтерактивного елемента.
- **`<label>` для кожного поля** (видимий або `aria-label`).
- **Контраст** тексту ≥ 4.5:1; видимі focus-стани (`:focus-visible`).
- **Жодних нативних `confirm/alert/prompt`** — лише `app.confirm()` / тости (правило вже дотримано).
- **Усі дані користувача через `app.escapeHTML()`** перед `innerHTML`.

---

## 8. Як перевіряти відповідність (чек-ліст)

Перед «готово» для будь-якої UI-зміни:
- [ ] Кольори — лише `var(--color-*)`, працює в темній темі
- [ ] Розміри шрифту — зі шкали (§3), один розмір на роль
- [ ] Радіус/тінь — токени; картка = `--radius-lg` + `--shadow-sm`
- [ ] Tap-target ≥ 44px
- [ ] Поля з підписом `.field-label`; дані екрановані
- [ ] Іконки Phosphor (емодзі лише ролі 👍/👁️)
- [ ] Не з'явився новий майже-дубль існуючого класу
- [ ] Перевірено на 360px + темна тема
